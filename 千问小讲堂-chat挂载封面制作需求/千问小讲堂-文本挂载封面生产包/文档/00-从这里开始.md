# 千问小讲堂 · 课程封面生产启动包

> **给谁用**：内容/运营同学（非技术）。  
> **干什么**：批量产出「钩子 + 标签 + 学名 + 生图指令」各 3 套（A/B/C），以及（数学类）底图压字合成。  
> **怎么用**：把本文件夹路径发给 Cursor，说一句开场话术即可（见 `发给Cursor的开场话术.md`）。

---

## 一、两条链路（先选一条）

| 链路 | 你要干什么 | 看哪份说明 |
|------|------------|------------|
| **A · 文案 + 生图指令** | 还没有底图，要产 A/B/C 钩子、标签、学名、生图指令 | 下文「四步」+ `学科分支说明.md` |
| **B · 挂载压图** | 已有 **学名·确认版 + url·确认版**，要线上封面 + 回写 CDN | **`挂载压图分支说明.md`**（桌面工程 `qianwen-cover-generator`） |

两条可连着做：先 A 出底图 → 内容确认 url → 再 B 压挂载封面。

### 链路 A：文案 + 生图（4 步）

| 步骤 | 你负责 | Cursor/脚本负责 | 产出 |
|------|--------|-----------------|------|
| **① 清单** | 提供 Excel/CSV（学科、年级、课程名、一句话介绍…） | 读懂表头、对齐字段 | 待生产清单 |
| **② 文案+生图指令** | 确认画风方向（数学/语文/成语…） | 按 Skill 写 A/B/C 四套字段 | 带 12 列 case 的 xlsx |
| **③ 出图** | 把「生图指令」交给生图同事/模型，选图 | 可协助批量 URL、命名规范 | 底图 jpg/png |
| **④ 数学压字（可选）** | 提供「需合成表」（钩子、标签、底图 url） | `compose_math.py` | 数学挂载 png |

### 链路 B：挂载压图（3 步）

| 步骤 | 你负责 | Cursor/脚本负责 | 产出 |
|------|--------|-----------------|------|
| **① 确认版表** | `学名·确认版`、`url·确认版`（成语还要钩子） | 检查列名、条数 | CSV/xlsx |
| **② 压图+上传** | 确认科普只压《》内篇目、不要钩子 | `run_kepu.py` / `run_style3.py` 等 | PNG + JPG |
| **③ 回写** | 把两列 URL 交给线上 | 脚本自动回填 | `封面压图压缩前/后url` |

**你通常**：链路 A 做 ①③ 决策；链路 B 只确认表和样例，其余交给 Cursor。

---

## 二、按学科选哪条路（30 秒判断）

| 学科/内容 | 画风 | 学名格式 | 标签 | 主要脚本/规范 |
|-----------|------|----------|------|----------------|
| **低龄数学** 1–3 年级 | Pixar 硅胶 3D | 课程短名 | 年级★关键词 | `cover_lib.py` + `batch_gen.py` |
| **高年级数学** 4–6 年级 | 瑞士国际 2.5D | 课程短名 | 年级｜关键词 | 同上 |
| **语文** 古诗词/课文/写作/科普 | 电影级中国风插画 | `[类型]《篇目》` 或仅篇目名 | 千问小讲堂 | `yuwen_lib.py` + `yuwen_data_*.py` |
| **趣学成语 / 故事成语** | 同语文中国风 | **成语名本身**（如 `守株待兔`） | 千问小讲堂 | `build_chengyu.py` |
| **数学压字** | 不重新生图，在底图上压钩子/标签 | — | 见 `字体规则.md` | `compose_math.py`（链路 A） |
| **挂载压图** | 已有确认版底图 url | 仅《》内或整段学名 | 科普无钩子 | **`挂载压图分支说明.md`**（链路 B） |

详细分支见 **`学科分支说明.md`** + **`挂载压图分支说明.md`**。

---

## 三、文件夹结构（本启动包）

```
千问小讲堂-封面生产启动包/
├── 00-从这里开始.md          ← 你正在读
├── SKILL.md                  ← Cursor Agent 主技能（可同步到 .cursor/skills/）
├── 工作流总览.md              ← 流程图 + 角色分工
├── 学科分支说明.md            ← 数学/语文/成语 文案链（链路 A）
├── 挂载压图分支说明.md        ← 确认版表 → 封面 URL（链路 B）★
├── 发给Cursor的开场话术.md    ← 复制粘贴即可开工
├── templates/                ← 新学科登记表、输出列说明
├── skills-引用/              ← 各学科规范 + **挂载压图-style3-摘要.md**
├── scripts-索引/             ← 链路 A 脚本索引 + **挂载压图脚本.md**
├── logs/                     ← 项目演进与历史会话纪要
├── inputs/                   ← 你把待生产的 xlsx/csv 放这里（建议）
├── outputs/                  ← 产出 xlsx 的推荐存放处
└── examples/                 ← 已验收样例索引
```

**可执行代码分两处**（不要挪走启动包，只做「说明书 + 索引」）：

| 链路 | 代码位置 |
|------|----------|
| A 文案/数学压字 | 上一级 `../scripts/`、`../assets/`、`../refs/` |
| B 挂载压图 | **`~/Desktop/qianwen-cover-generator/`**（备份见 `../_qcg/qianwen-cover-generator/`） |

---

## 四、依赖（只需装一次）

在终端进入 `output/cover-generator/refs/` 执行：

```bash
pip3 install -r requirements.txt
```

压图还需要字体文件，已在 `../assets/`（造字工房元黑、阿里巴巴普惠体等）。  
数学压图详见 **`../字体规则.md`**。

---

## 五、推荐工作节奏（新学科第一次）

1. 填写 **`templates/新学科需求登记表.md`**
2. 把源表放进 **`inputs/`**
3. 打开 Cursor，粘贴 **`发给Cursor的开场话术.md`** 里对应模板
4. 先要 **3 条样例**（小低/小高/语文各一）→ 你确认画风和钩子风格
5. 再 **全量 50/200/500 条** → Cursor 自检后给 xlsx
6. 生图同事出图 → 如需压图，再走数学/语文压图脚本

---

## 六、已产出的参考文件（仓库内）

| 文件 | 说明 |
|------|------|
| `../outputs/千问小讲堂-线上内容汇总-0528-FINAL.xlsx` | 语文等已合并 3-case 的主表 |
| `../outputs/趣学成语-封面文案-3cases.xlsx` | 趣学成语 50 条 × A/B/C |
| `../outputs/趣学成语-样例3条.md` | 成语封面样例 |
| `../字体规则.md` | 小低/小高压图字体与排版铁律 |

---

## 七、让 Cursor 自动认出本启动包（可选）

把本目录下的 `SKILL.md` 复制或链接到项目技能目录：

`/.cursor/skills/qianwen-cover-production/SKILL.md`

之后你说「按千问封面启动包做 XX 学科」即可触发。

---

**维护**：内容策略变更时，优先改 `skills-引用/` 摘要和 `SKILL.md`，并记一笔到 `logs/项目演进记录.md`。
