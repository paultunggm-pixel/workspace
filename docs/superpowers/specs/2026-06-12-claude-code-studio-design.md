# Claude Code Studio — 产品设计规格书

> 版本：v2.1 | 日期：2026-06-13 | 状态：全部 UI 设计定稿，待实现规划

> v2.1 新增：🔌 服务 Tab（MCP 管理）+ 服务目录（30+ 典型服务）

---

## 1. 产品概述

### 1.1 定位

Claude Code Studio 是一款 macOS 原生桌面应用，作为 Claude Code 的外壳程序，将 AI Agent 能力**产品化、可视化、易用化**。设计理念借鉴 Codex 客户端——以需求/设计人员为本，降低工程师知识门槛。

### 1.2 核心目标

- **信息聚合**：将分散在终端、VS Code、浏览器中的信息集中到一个窗口
- **项目管理**：以项目为中心（而非会话），建立分类体系
- **可视化预览**：方案/设计/代码/部署一站预览
- **模型管理**：完全内建的模型配置，零外部依赖
- **酷炫体验**：SwiftUI 原生，流畅动画，现代设计

### 1.3 技术选型

| 项 | 选择 |
|------|------|
| 框架 | SwiftUI (macOS 13+) |
| 语言 | Swift |
| 密钥存储 | macOS Keychain (`SecItem` API) |
| 配置存储 | `~/.claude-code-studio/` (自有 JSON 格式) |
| 与 cc 兼容 | 双向同步 `~/.claude/settings.json` |

---

## 2. 窗口布局

### 2.1 整体结构（Dashboard Hub）

```
┌──────────────┬──────────────────────────────┬─────────────┐
│  左侧栏 280px │       中央区 (flex)           │ 右侧栏 300px │
│              │                              │             │
│ 🧠 模型引擎    │ 💬📋🎨📝🚀  Tab 切换         │ 🚀 部署管理  │
│              │                              │             │
│ 📁 项目管理    │  当前 Tab 内容                │ 部署目标勾选  │
│ ├ 分类1      │                              │ 文件确认     │
│ │ ├ 项目A    │                              │ 一键部署按钮  │
│ │ │ ├ 会话1  │                              │ 进度面板     │
│ │ │ └ 会话2  │                              │ 部署历史     │
│ │ └ 项目B    │                              │ 快捷链接     │
│ └ 分类2      │                              │             │
└──────────────┴──────────────────────────────┴─────────────┘
│                        状态栏 (28px)                        │
└────────────────────────────────────────────────────────────┘
```

### 2.2 响应式规则

- 左右侧栏均可折叠（快捷键 `⌘⇧L` / `⌘⇧R`）
- 最小窗口宽度：900px
- 折叠态左侧栏仅显示图标（36px）
- 推荐分辨率：1400×900 以上

---

## 3. 左侧栏 — 模型引擎区

### 3.1 主模型下拉选择器

- 展开后按**提供商分组**（DeepSeek / Anthropic / OpenAI 兼容）
- 当前选中项标 ✓ + 蓝色高亮
- 底部「+ 添加模型提供商...」入口
- 支持键盘导航（↑↓ Enter）

### 3.2 三键快切

- 三个预设模型快捷按钮：⚡ Flash / 🧠 Pro / 🔮 Opus
- 当前激活按钮蓝色边框高亮
- 切换即时生效，影响后续对话

### 3.3 余额 / 用量卡片

- 显示当前模型提供商的余额（大字体金额 + 颜色：绿 >50%、黄 10-50%、红 <10%）
- 月度用量进度条
- 今日调用次数 + Token 消耗
- 底部链接到 provider 用量页面（如 `platform.deepseek.com/usage`）
- Token 过期后显示「⚠️ 授权已过期」提示重新授权

### 3.4 完全内建的模型设置

#### 设计原则

- **零依赖**：不需要用户安装 `cc switch`
- **自有配置**：`~/.claude-code-studio/providers.json`
- **cc 兼容**：首次启动自动导入 `~/.claude/settings.json` 已有配置，每次修改双向同步
- **密钥安全**：100% macOS Keychain，不落盘

#### 添加提供商流程

```
步骤① 选择提供商 → 步骤② 输入 API Key → 步骤③ 验证 & 启用
```

- 步骤①：预设列表（DeepSeek / Anthropic / OpenAI 兼容）+ 自定义端点
- 步骤②：粘贴 Key，立即通过 `SecItemAdd()` 入 Keychain，输入框显示 `sk-●●●●●●●●d8f2`
- 步骤③：自动调用 provider 的 models list API 测试连接，拉取余额

#### 模型设置面板（⚙️ 管理入口）

- 每个提供商卡片：名称 · 连接状态 · API Key 摘要 · 默认模型选择
- 展开后：Key 管理（👁️显示 / 删除）+ 可用模型列表 + 余额信息
- 「+ 添加」按钮 → 触发三步引导

### 3.5 首次初始化引导

- 检测到 `providers.json` 为空 → 模型选择器位置显示引导卡片
- 引导内容：「🧠 尚未配置模型」→ 点击「+ 添加提供商」→ 进入三步设置
- 不使用 `cc switch`，完全在 App 内完成

### 3.6 数据流

```
~/.claude/settings.json  ←→  App 双向同步 (当前模型选择)
~/.claude-code-studio/   ←   App 自有配置 (providers + preferences)
macOS Keychain           ←   API Key (仅 App 可读，不落盘)
provider API             →   App 定时拉取 (余额, 每小时刷新)
```

---

## 4. 左侧栏 — 项目管理区

### 4.1 数据结构

三级树形：**分类 → 项目 → 会话**

```
📁 项目管理
├─ 📂 数据分析 (分类)
│   ├─ 🏆 2026世界杯 (项目)
│   │   ├─ 💬 小组赛预测模型优化 (会话)
│   │   ├─ 💬 淘汰赛对阵分析 (会话)
│   │   └─ + 新建会话
│   └─ 📊 解题一致性评测 (项目)
├─ 📂 产品开发 (分类)
│   └─ 🔧 Claude Code Studio (项目)
└─ 📂 未分类 (默认分类，不可删除)
```

### 4.2 分类操作

| 操作 | 行为 |
|------|------|
| 新建分类 | 点击 `+📂` → 输入名称 → 创建空分类 |
| 删除分类 | 仅当分类内无项目时可删，否则提示「请先移走项目」 |
| 展开/收起 | 点击分类名切换 |

### 4.3 项目操作

| 操作 | 行为 |
|------|------|
| 新建项目 | 点击 `+📁` → 输入项目名 → 选择所属分类 |
| 移动项目 | 右键 →「移动到...」→ 选择目标分类 / 新建分类 |
| 删除项目 | **三次确认**（见 4.4） |
| 重命名 | 双击项目名编辑 |
| 更换图标 | 右键 → emoji picker |

### 4.4 删除项目三次确认

1. **第1次**：输入项目名称确认
2. **第2次**：显示警告「包含 N 个会话，删除后不可恢复」
3. **第3次**：5 秒倒计时确认按钮

### 4.5 会话操作

- 展开项目 → 显示会话列表，按时间分组（今天 / 昨天 / 更早）
- 点击会话 → 中央区加载对话历史
- 新建会话：「+ 新建会话」按钮 → 中央区空白对话页
- 会话状态：●活跃 / ◐暂停 / ○完成

### 4.6 右键菜单（项目）

```
📂 在 Finder 中打开
✏️ 重命名项目
🎨 更换图标
──────────────
📦 移动到...       →
──────────────
📤 导出会话记录
──────────────
🗑️ 删除项目         (红色)
```

---

## 5. 中央区 — 全宽工作区

> 右侧部署栏已移除，280px 空间释放给中央区。

### 5.1 Tab 列表

`💬 会话 | 📋 方案 | 🎨 预览 | 📝 代码 | 🚀 发布`

### 5.2 💬 会话 Tab（含二级会话切换）

- 默认视图，Claude Code 核心交互界面
- 消息气泡：Claude（左侧，琥珀色头像）+ 用户（右侧，蓝色边框）
- 产物卡片：对话中生成的文件以卡片形式展示，点击可跳至预览/代码 Tab
- 进度步骤：部署等异步操作显示实时步骤（✅⏳○）
- 输入区：底部固定，支持 `⌘↵` 发送、`⇧↵` 换行、📎 拖入文件
- 操作栏：每条 Claude 消息下方有 👍👎 / 📋复制 / 🔄重新生成

#### 5.2.1 双层 Tab 结构

一级 Tab `💬 会话` 下方有二级会话标签栏：

```
[小组赛预测模型优化 ×] [淘汰赛对阵分析 ×] [数据源调研 ×] [+ 新会话]
```

- 点击会话 Tab 切换对话内容
- 每个会话 Tab 有 × 关闭按钮
- 仅剩一个会话时 × 可点击但被拦截，提示「不能删除最后一个会话」
- `+ 新会话` 创建空白会话

#### 5.2.2 会话双方身份

| 角色 | 名称 | 头像 |
|------|------|------|
| AI | Claude Code | 官方 Logo 圆形（琥珀色边框 1px #d97757） |
| 用户 | 我 | 紫色渐变圆形首字母（默认 P，后续支持自定义上传） |

#### 5.2.3 快捷操作按钮

输入框上方三个按钮：

| 按钮 | 作用 | 写入目标 |
|------|------|------|
| 📜 沉淀到 CLAUDE.md | 提取对话策略写入全局配置 | `~/Documents/Claude/CLAUDE.md` |
| 📋 沉淀到本项目 Skills | 提取对话策略写入当前项目 Skill | 当前项目 .md |
| 🗜 压缩上下文 | 压缩当前对话为摘要，释放 Token | 替换会话上下文 |

- 沉淀到 CLAUDE.md 和沉淀到本项目 Skills 的区别：前者全局生效，后者仅当前项目
- 压缩上下文与沉淀的配合：先沉淀关键策略 → 再压缩释放空间

### 5.3 📋 方案 Tab

- AI 自动维护的活文档，随对话实时更新
- 始终反映当前最新方案，不保留历史版本
- 内容结构由 AI 根据项目特点自行决定，非固定模板
- 操作栏：📥 导出 MD（导出当前最新状态）
- 方案内容来源：项目概览 .md + 对话内容 + 生成文件
- 中央 Tab 标签上的蓝点提示「有更新」

### 5.4 🎨 预览 Tab

- 多类型文件实时预览：
  - HTML → 内嵌 WebView 渲染
  - 图片 (png/jpg/svg/gif) → 图片查看器
  - Markdown → 渲染预览
- 底部文件标签栏：切换当前项目的不同产物文件
- 工具栏：🔄刷新 / 🔗浏览器打开 / 📱移动端视口
- 切换文件时热刷新

### 5.5 📝 代码 Tab

- 语法高亮代码视图（树状语法着色）
- Diff 模式：新增行绿色背景、删除行红色背景
- 底部文件标签栏：切换当前项目的不同代码文件
- 工具栏：📋复制 / ↗️ VS Code（一键在外部编辑器打开）
- 轻量定位——不替代 VS Code 的编辑能力

### 5.6 🚀 发布 Tab

> 原名「部署」。改为「发布」以覆盖 Web / 移动 App / 桌面客户端三种项目类型。

#### 5.6.1 自动识别项目类型

App 根据项目概览中配置的「产物类型」自动切换操作面板：

| 项目类型 | 产物 | 发布模式 |
|------|------|------|
| 🌐 Web 产品 | .html, 静态站点 | 上传到 OSS / Pages |
| 📱 移动 App | .ipa, .apk, .aab | 构建安装包 → 分发到渠道 |
| 💻 桌面客户端 | .dmg, .exe, .AppImage | 构建安装包 → 分发到渠道 |

#### 5.6.2 统一两阶段模式

所有类型均遵循：**🔨 构建 → 📤 分发**

##### Phase 1: 🔨 构建

| 类型 | 构建目标 | 本地测试 |
|------|------|------|
| 🌐 Web | 无需构建，直接预览 | 🔗 浏览器打开 |
| 📱 移动 App | iOS .ipa · Android .apk/.aab | 📥 安装到本地手机（生成二维码扫码安装） |
| 💻 桌面 | macOS .dmg · Windows .exe · Linux .AppImage | 📥 本地安装运行（自动挂载 .dmg 拖拽安装） |

##### Phase 2: 📤 分发到渠道

| 类型 | 分发渠道 |
|------|------|
| 🌐 Web | ☁️ OSS · 🌐 GitHub Pages |
| 📱 移动 App | 🍎 TestFlight · 📱 App Store · 🤖 Google Play · 🏪 厂商商店 |
| 💻 桌面 | 🐙 GitHub Releases · 🍎 Mac App Store · 🏪 软件管家 · 🌐 官网下载 |

#### 5.6.3 发布面板布局

```
🚀 发布

📍 发布目标      [☑ OSS] [☑ Pages]
📦 待发布文件      📊 evaluation_report.html 46KB
                  [🚀 一键发布] [👁 预览]

🌐 线上地址       🟢 OSS → https://...        [📋] [🔗]
                 🟢 Pages → https://...      [📋] [🔗]

📜 发布记录       ● 今天 10:36  ✅ OSS ✅ Pages
                 ● 昨天 15:42  ✅ OSS ❌ Pages (构建失败)

📦 可发布产物     [report.html] [polymarket.html]
```

#### 5.6.4 本地安装交互流程

**📱 安装到本地手机：**
```
① 点击按钮 → ② 检查构建（过期则自动构建）
→ ③ 生成二维码（iOS OTA / Android 直接下载）
→ ④ ✅ 手机已安装
```

**💻 本地安装运行：**
```
① 点击按钮 → ② 检查构建（版本不变则问是否重用）
→ ③ .dmg 自动挂载，弹出拖拽安装界面
→ ④ ✅ 已安装 [启动应用] [Finder 中显示]
```

### 5.7 Tab 间数据联动

| 触发 | 影响 |
|------|------|
| 对话中产生方案 | 📋 Tab 自动更新 |
| 对话中生成文件 | 🎨/📝 Tab 自动选中新文件并渲染 |
| 修改代码保存 | 📝 Tab 显示 Diff，🎨 Tab 热刷新 |
| 点击产物卡片 | 按文件类型跳 🎨（html/png）或 📝（.py/.js） |
| 部署完成 | 🚀 Tab 更新链接 + 时间线 |

### 5.8 🔌 服务 Tab（MCP 管理）

#### 5.8.1 概述

统一管理所有 MCP 和非 MCP 服务连接。Tab 名称用「服务」而非「MCP」——用户不需要知道协议细节，只需知道这里管理所有连接的能力。

#### 5.8.2 布局结构

```
┌─ 摘要栏 ──── 共 N 项 / X 正常 / Y 异常 / Z 待授权 ──┐
├─ 🌐 服务A         描述文字        今日N次  🟢已连接 ──┤
├─ 🕷 服务B         描述文字        今日N次  🟢已连接 ──┤ ← 点击展开
│  ┌──────详情─────────┬─────────工具──────────┐       │
│  │ 端点/认证方式/用量  │ tool1 tool2 tool3…   │       │
│  │ [重连][日志][文档]  │ [断开连接]            │       │
│  └──────────────────┴────────────────────────┘       │
├─ 🎬 服务C         描述文字        今日N次  🟡异常   ──┤
└─ + 添加服务 ──────────────────────────────────────────┘
```

#### 5.8.3 服务状态

| 状态 | 含义 |
|------|------|
| 🟢 已连接 | API Key 有效，近期调用成功 |
| 🟡 异常/限流 | 额度用尽、限流中、响应慢 |
| 🔴 连接失败 | Key 无效、端点不可达 |
| ⚪ 待授权 | 已安装但未配置 Key/OAuth |

#### 5.8.4 服务目录（添加服务）

点击 `+ 添加服务` 弹出服务目录，按六大分类浏览：

| 分类 | 典型服务 |
|------|------|
| 🛠 开发工具 | GitHub (gh CLI), Docker, GitLab |
| 🗄 数据存储 | 阿里云 OSS, PostgreSQL, Redis, S3/R2, SQLite, MongoDB |
| 🤖 AI & 搜索 | DeepSeek, Exa, Firecrawl, Together AI, Algolia, Pinecone |
| 🎨 内容 & 媒体 | Runway, Adobe Creative, Resend, Slack, 钉钉, Notion |
| 🚀 部署 & 运维 | GitHub Pages, Vercel, Cloudflare, Sentry, Stripe |
| 🔌 自定义 | MCP 协议, HTTP API, 本地脚本, 导入 .mcp.json |

- 已安装的服务标 `✓ 已安装` 角标
- 未安装的点击即可引导配置
- 总计 30+ 服务覆盖典型场景
- 自定义接入覆盖所有未列出的需求

---

## 6. 全局组件

### 7.1 顶部栏

- 左侧：Claude Code 官方 Logo (SVG) + "Claude Code Studio"
- 右侧：🟢 连接状态指示器 · 设置按钮 · 🌙 暗色模式切换

### 7.2 底部状态栏

- 左侧：当前项目路径 · 自动保存状态
- 右侧：会话时长 · Token 用量 · 当前模型名称

---

## 7. 安全设计

### 8.1 密钥管理铁律

| 原则 | 实现 |
|------|------|
| 🚫 不存明文 | API Key 绝不落盘（plist / UserDefaults / JSON） |
| 🔐 Keychain | 仅通过 `SecItemAdd/Delete/Copy` 操作 macOS 钥匙串 |
| 🔗 OAuth | 用量查询通过系统 Safari 完成 OAuth，App 只拿 auth code |
| 🗑️ 登出即毁 | `SecItemDelete` 彻底清除，无残留 |
| 👁️ 掩码显示 | UI 中仅显示 `sk-●●●●●●●●d8f2`（后4位） |

### 8.2 第三方登录

- 用量数据（如 DeepSeek 余额）通过 OAuth 2.0 获取
- 登录完全在 `platform.deepseek.com` 完成（系统 Safari）
- App 不嵌入 WebView，无法捕获用户在第三方网站的任何输入
- 回调仅返回一次性 auth code（5分钟过期）

---

## 8. 与 Claude Code CLI 的关系

### 9.1 数据互通

- 读取 `~/.claude/settings.json` 获取模型配置
- 写入 `~/.claude/settings.json` 同步模型选择
- 兼容 `cc` CLI 的所有命令

### 9.2 配置独立性

- App 主配置在 `~/.claude-code-studio/`
- 首次启动自动从 cc 配置导入
- 双方均可独立修改，App 每次启动时检测差异并合并

---

## 9. CLAUDE.md / 项目.md / Skill 概念体系

### 10.1 三者定位

| | CLAUDE.md | 项目概览 .md | Skill |
|------|------|------|------|
| **作用域** | 全局，所有会话 | 仅本项目内 | 被调用时生效 |
| **内容** | Claude 的底层行为方式 | 项目背景、约束、配置 | 特定任务的执行流程 |
| **谁定义** | 用户定义（一次） | 用户定义（每项目） | 插件提供或自建 |
| **变更频率** | 很少改 | 随项目演进而更新 | 按需创建 |
| **类比** | 员工手册 | 项目立项书 | SOP 操作手册 |

### 10.2 产品中的名称

- CLAUDE.md → **「全局设置」**，入口在左侧栏，与模型引擎并列
- 项目 .md → **「项目概览」**，入口在项目管理区，每个项目右键→项目概览
- Skill → **「技能插件」**，全局设置下的子页面

---

## 10. CLAUDE.md 全局设置

### 11.1 设计原则

- **人性化编辑**：标签点选代替手写 yaml，开关代替 true/false，自然语言文本框代替严格 schema
- **保留原始入口**：每个设置面板提供「查看原始 MD」按钮
- **Apple 风格排版**：iOS Settings 式分组列表，白色 group-body + 灰色背景分组

### 11.2 设置板块

#### 交流偏好
- **工作语言**：标签单选（简体中文 / 繁體中文 / English / + 其他）
  - 点击「+ 其他」→ 内联输入框弹出 → 确认后加入标签列表
- **回复风格**：标签单选（简洁 / 适度 / 详尽）

#### 权限与安全（合并板块，含三个子板块）

**询问频率**（radio 单选，描述右置）：
- 关键确认（默认）：隐私、安全、文件删除等敏感操作询问，常规自主
- 每步询问：所有操作执行前确认
- 高度自主：AI 全权执行，可随时暂停或回滚

**读写权限**（radio 单选，二选一）：
- 指定授权读写单一目录（默认）：Claude 仅能读写指定文件夹
  - 选中后显示路径输入行 + 📂 选择按钮，默认 `~/Documents/Claude/`
- 全局授权读写本地文件数据：Claude 可访问用户目录下任意文件

**安全策略**（标签，多选）：
- 不存储密钥 / 外发前确认

#### 编码风格（开关列表，副标题右侧并排）
- 动手前先对齐需求
- 简单优先
- 精准修改
- 修改后自动代码审查

#### 自定义规则（文本框，min-height: 144px）
- 自然语言补充全局规则

### 11.3 排版规范

- 一级标题 `.group-header`（11px W600 灰色）作为板块标题
- 二级标题 `.row-label`（13px）作为字段名，`.row-hint`（11px 灰色）在右侧
- 白色 `.group-body` 圆角 10px，内部 row 之间 1px 淡灰分隔线
- 子板块之间用 1px rgba(0,0,0,0.06) 分隔线

---

## 11. 项目概览 .md

### 12.1 基本信息

- **项目名称**：直接输入框（非点击编辑），对齐下方标签垂直线
- **项目描述**：textarea，Claude 进入项目时自动加载

### 12.2 项目配置（标签左对齐，标签标题统一 58px 宽度）

- **数据源**：标签 + 右对齐「+ 添加」
- **技术栈**：同上
- **产物类型**：同上
- **部署目标**：同上

### 12.3 项目规则（textarea，min-height: 144px）

- 仅在本项目内叠加生效的自然语言指令

---

## 12. 对话区快捷操作

### 14.1 三个快捷按钮

输入区上方三个操作按钮，提高对话过程中的知识沉淀效率：

| 按钮 | 作用 | 写入目标 |
|------|------|------|
| 📜 **沉淀到 CLAUDE.md** | 将对话中的通用优化策略提取并追加到全局 CLAUDE.md | `~/Documents/Claude/CLAUDE.md` |
| 📋 **沉淀到本项目 Skills** | 将对话中的项目特定策略提取并追加到当前项目 .md | 当前项目的项目 Skill 文件 |
| 🗜 **压缩上下文** | 压缩当前对话历史为摘要，释放 Token | 替换当前上下文窗口 |

### 14.2 三个按钮的分工

- **沉淀到 CLAUDE.md** vs **沉淀到本项目 Skills**：前者写入全局规则（所有项目生效），后者写入当前项目规则（仅本项目生效）
- **压缩上下文** vs **沉淀**：压缩是清缓存（临时），沉淀是写手册（持久）；两者经常配合——先沉淀再压缩

### 14.3 按钮样式

- 白底灰边框，8px 圆角，1px 阴影
- 11px 字号，500 字重
- 与输入框同行，位于输入区上方

---

## 13. 模型提供商配置

### 15.1 支持的提供商

| 排序 | 提供商 | 图标来源 | 币种 |
|------|--------|------|------|
| 1 | DeepSeek | 官方 whale SVG（仅图标，不含文字） | ¥ |
| 2 | Qwen | qwenlm.github.io/img/logo.png | ¥ |
| 3 | Anthropic | @anthropic-ai/sdk/.github/logo.svg (Claude 火花) | $ |
| 4 | OpenAI 兼容 | simple-icons CDN (六边形花) | $ |

### 15.2 余额显示规则

- DeepSeek / Qwen → 显示 ¥
- Anthropic / OpenAI → 显示 $
- 每小时从 provider API 自动刷新

---

## 14. 项目管理区更新

### 16.1 项目菜单入口

- 每个项目右侧 `⋯` 按钮 → 展开菜单：[📋 项目概览 / 📦 移动到… / ✏️ 重命名 / 🗑️ 删除项目]
- 右键项目 → 同样菜单

### 16.2 会话与中央区关系

- 侧栏会话列表 = 会话切换器
- 中央 💬 Tab = 当前活跃会话的对话内容
- 点击不同会话 → 中央所有 Tab（对话/方案/预览/代码/部署）同步切换上下文
- 当前活跃会话标蓝色 + 「← 当前」标签

- [ ] 应用名称终稿：Claude Code Studio？
- [ ] 应用图标设计（基于 Claude Code 官方 Logo 再设计）
- [ ] 是否支持多窗口（同时打开多个项目）？
- [ ] 是否需要内置终端面板（嵌入 CLI）？
- [ ] 是否支持插件扩展？

---

## 附录：设计产出文件索引

所有视觉设计稿保存在 `~/Documents/Claude/.superpowers/brainstorm/` 中：

| 文件 | 内容 |
|------|------|
| `approaches.html` | 三种产品架构方案对比 |
| `layout-v1.html` / `layout-v2.html` | 整体布局迭代 |
| `model-engine.html` | 模型引擎区下拉 + 快切 + 余额 |
| `auth-model.html` / `auth-clarify.html` | 授权流程与安全架构 |
| `model-integration.html` | 三种模型配置方案对比 |
| `model-native.html` | 完全内建设置面板 |
| `project-tree.html` | 项目树形结构 v1 |
| `project-v3.html` | 项目管理区 v3（分类-项目-会话） |
| `center-preview.html` / `center-v2.html` | 中央预览区设计 |
| `deploy-panel.html` | 右侧部署管理面板 |
| `apple-style.html` | Apple 设计语言全窗口布局 |
| `settings-redesign.html` | CLAUDE.md + 项目概览设置面板（定稿版） |
| `claudemd-v3.html` | CLAUDE.md 管理器迭代 v3 |
| `lang-other.html` | 「+ 其他」语言输入交互演示 |
| `toggle-states.html` | 开关状态对比演示 |
| `model-init.html` | 模型初始化三步授权流程（真实品牌 Logo） |
| `apple-style.html` | Apple 设计语言全窗口布局 |
| `merged.html` | 首页全布局定稿（移除右侧栏、双层 Tab、快捷按钮） |
| `tabs-v2.html` | 五 Tab 详细设计（方案/预览/代码/发布整合版） |
| `release-types.html` | 发布三种类型对比（Web/移动App/桌面） |
| `local-install.html` | 本地安装运行交互流程（手机+桌面） |
| `service-tab.html` | MCP 服务管理 Tab v1 |
| `service-v2.html` | 服务管理 Tab v2（单列展开式） |
| `service-catalog.html` | 服务目录（6 大类 30+ 服务） |
